<template>
  <section>
    <h1>Simulation Viewer</h1>
    <p>
      Live hex map render of a running <code>SimulationRun</code>, order entry, step/play controls,
      and the event log, driven over the <code>/api/simulations/:id/stream</code> WebSocket. Phase 4
      — see <code>design_doc.md</code>.
    </p>
  </section>
</template>
